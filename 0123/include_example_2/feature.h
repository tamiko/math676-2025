/*
 * Ensure that this header file is only ever included once:
 */

#pragma once

/*
 * Declare the existence of a function "foo" with the signature "void
 * foo(void)", meaning the function takes no arguments and returns no
 * value.
 *
 * With this the compiler is able call the function.
 */

void foo();

